{
  "name": "cupkit-mcp-server",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server exposing World Cup data for AI agents — live fixtures, scores, standings via OpenLigaDB",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
